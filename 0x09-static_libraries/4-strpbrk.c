#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strpbrk - find the first char of first string and
 * matches it to any char of second string
 * @s: character of string
 * @accept: character to accept
 * Return: always zero
 */

char *_strpbrk(char *s, char *accept)

{
	return (0);
}
