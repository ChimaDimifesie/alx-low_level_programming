#include "main.h"

#include <stdio.h>

#include <stdlib.h>

#include <ctype.h>

#include <string.h>

/**
 * _puts - used to write a line or string to the output
 * stream ( stdout ) that is up to, but
 * does not include, the null character.
 * @s: string to append
 * Return: NULL
 */

void _puts(char *s)
{
	_puts("NULL");
}
