#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - checks the length of a string
 * @s: string to be checked
 * Return: Always zero
 */

int _strlen(char *s)
{
	return (0);
}
