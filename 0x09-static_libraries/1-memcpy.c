#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _memcpy - copy specified bytes of memory from src address to dest address
 * @dest: destination address
 * @src: source address
 * @n: bytes of memory to copy
 * Return: always zero
 */

char *_memcpy(char *dest, char *src, unsigned int n)

{
	return (0);
}
