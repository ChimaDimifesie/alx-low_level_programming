#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - checks if a number is absolute
 * @n: the number to check
 * Return: Always zero
 */

int _abs(int n)
{
	return (0);
}
