#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - checks whether a character is numeric character (0-9) or not.
 * @c: character to check
 * Return: always zero
 */

int _isdigit(int c)
{
	return (0);
}
